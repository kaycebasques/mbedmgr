import setuptools

setuptools.setup(
    name='embeddingsmanager',
    version='0.0.1',
    packages=['embeddingsmanager'],
    install_requires=[
        'beautifulsoup4==4.12.2',
        'requests==2.31.0'
    ]
)
